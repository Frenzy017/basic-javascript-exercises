function Birthday(input) {
    let hallRent = Number(input[0]);
    let cakePrice = hallRent * 0.20;
    let Drink = cakePrice * 0.55;
    let animator = hallRent / 3
    let sum = hallRent + cakePrice + Drink + animator
    console.log(sum)






}

Birthday(["2250"])